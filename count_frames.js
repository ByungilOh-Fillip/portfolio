const fs = require('fs');
// Let's just output HTML with the image scaled up so I can guess, or just use linear animation but with a step-like framerate?
// No, I can just measure it!
console.log("We'll use a continuous scrolling animation but make it look like 16fps using JS or CSS.")
